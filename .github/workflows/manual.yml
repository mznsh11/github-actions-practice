name: Manual Workflow

on:
  workflow_dispatch:
    inputs:
      name:
        description: "Enter your name"
        required: true

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello, ${{ github.actor }}!"